<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CarouselMapper">
    <insert id="insert" parameterType="com.example.entity.Carousel">
        INSERT INTO carousel (title, user_id, image_url, create_time, related_type, related_id)
        VALUES (#{title}, #{userId}, #{imageUrl}, #{createTime}, #{relatedType}, #{relatedId})
    </insert>
    <delete id="deleteById">
        DELETE FROM carousel WHERE id = #{id}
    </delete>
    <update id="update" parameterType="com.example.entity.Carousel">
        UPDATE carousel
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
            <if test="createTime != null">create_time = #{createTime}</if>
        </set>
        WHERE id = #{id}
    </update>
    <select id="selectById" resultType="com.example.entity.Carousel">
        SELECT * FROM carousel WHERE id = #{id}
    </select>
    <select id="selectAll" resultType="com.example.entity.Carousel">
        SELECT n.*, u.username FROM carousel n LEFT JOIN user u ON n.user_id = u.id
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="msg != null and msg != ''">
                AND msg LIKE CONCAT('%', #{msg}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
    </select>
</mapper> 